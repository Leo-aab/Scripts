## SHKIT 

I will put some Shell scripts to use for config your computer, every scripts needs sudo (admin) permission, so a example to use it:

```bash
sudo chmod +x ./example.sh 
```
and

```bash
sudo  ./example.sh
```

